{
  "hosting": {
    "public": "build",
    "rewrites": [
      { "source": "/api/set_user_name", "function": "setUserName" },
      { "source": "**", "destination": "/index.html" }
    ]
  },
  "firestore": {
    "rules": "firestore.rules"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
